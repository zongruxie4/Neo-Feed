# Opaque Overlay Transition

Removes the per-frame overlay window fade and defers the normal overlay hide until the panel has finished sliding closed. Launcher scroll progress and transparent-overlay behavior are unchanged.

Verification: automated checks pending; live device verification remains pending.
